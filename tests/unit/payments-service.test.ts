import { jest } from '@jest/globals';
import * as paymentsService from '../../src/services';
import * as paymentsRepository from '../../src/repositories/payments-repository';

describe('test', () => {
  it('test', async () => {
    expect(1).toBe(1);
  });
});
