import { jest } from '@jest/globals';
import * as hotelsService from '../../src/services';
import * as hotelsRepository from '../../src/repositories/hotels-repository';

describe('test', () => {
  it('test', async () => {
    expect(1).toBe(1);
  });
});
