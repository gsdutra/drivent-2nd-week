import { jest } from '@jest/globals';
import * as ticketsService from '../../src/services';
import * as ticketsRepository from '../../src/repositories/tickets-repository';

describe('test', () => {
  it('test', async () => {
    expect(1).toBe(1);
  });
});
